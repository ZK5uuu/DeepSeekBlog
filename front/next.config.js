/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'localhost', 'm.media-amazon.com', 'image.tmdb.org'],
  },
};

module.exports = nextConfig; 